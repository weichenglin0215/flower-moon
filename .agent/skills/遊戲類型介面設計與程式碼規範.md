---
name: game-interface-specification
description: 遊戲類型介面設計規範，用於「花月」專案中的小遊戲開發。
---

# 🎮 遊戲類型介面設計規範 (Game Interface Specification)

本規範定義了「花月」專案中所有遊戲類型頁面（如慢思快選、飛花令等）的介面設計、欄位分配與命名規則，旨在確保未來新增遊戲時具有高度的一致性與視覺品質。

## 0. 現有遊戲一覽（截至 2026-05）

| 遊戲 | 中文名稱 | 類型 | 難度設定主要 Key |
|------|---------|------|----------------|
| game1 | 慢思快選 | DOM | timeLimit, poemMinRating, maxMistakeCount, answerAtLine, minMaskCount, maxMaskCount |
| game2 | 飛花令 | DOM | timeLimitRate, poemMinRating, maxMistakeCount, answerAtLine, grid, questionCount |
| game3 | 字爬梯 | DOM | poemMinRating, maxMistakeCount, sentenceMinRating, minOptions, maxOptions, isStrictOrder, incrementSpeed, maxSpeed |
| game4 | 眾裡尋他千百度 | DOM | timeLimitRate, poemMinRating, maxMistakeCount, answerAtLine, maxMaskCount, maxAddDecoyChars, showDelay, singleCharReaction |
| game5 | 詩詞小精靈 | Canvas | timeLimitRate, poemMinRating, maxMistakeCount, monsters, answerLen, hintDuration, lostInt, lostDur |
| game6 | 詩陣侵略 | Canvas | timeLimitRate, poemMinRating, maxMistakeCount, fireRate, baseSpeed, speedInc, maxSpeed, lineCount |
| game7 | 青鳥雲梯 | Canvas | timeLimitRate, poemMinRating, maxMistakeCount, g, jump, width, maxDist, heightVar, move, speed, minChars, maxChars, stopOnLand |
| game8 | 一筆裁詩 | DOM | timeLimitRate, poemMinRating, maxMistakeCount, hints, splitPath, obstacles, decoyPool, minLines, maxChars |
| game9 | 詩韻鎖扣 | DOM | timeLimit, poemMinRating, bolts, emptyBolts, hasHint, undo, moveLimit, exchangeQuantity, totalNumberOfExchange, color |
| game10 | 擊石鳴詩 | Canvas | poemMinRating, growRate, dropInterval, dropStep, paddleBase, balls, blackHP, lineHeight, minLines, minChars, maxChars, maxCharsInLine, ballSpeed, ballMaxSpeed |
| game11 | 翻墨識蹤 | DOM | poemMinRating, maxMistakeCount, rows, cols, mode, feedback, minLines, maxLines, minChars, maxChars, slowFlipChars, passChars, revealStep |
| game12 | 疏影橫斜 | DOM | timeLimit, poemMinRating, maxMistakeCount, minShowCount, maxShowCount, minTotalHideCount, memorySeconds, isSequentialOpen, isSequentialHide, hasDistractors, showDelay, hideMode, total, cols |
| game13 | 人事時地 | DOM | timeLimitRate, poemMinRating, maxMistakeCount, metaHideCount, charHideCount, metaDistractors, charDistractors |
| game14 | 步步驚心 | DOM | timeMutiply, poemMinRating, maxMistakeCount, minChars, maxChars |

---

## 1. 介面設計與風格
*   **視窗適配系統整合**：所有遊戲介面必須位於 `screen_adaptive.css` 定義的 `#stage` 容器內，並遵循 `screen_adaptive.js` 計算出的縮放比例。確保這些覆蓋層在任何螢幕尺寸下都能等比例縮放並置中顯示。確保在不同螢幕下維持一致的遊戲比例。
*   **視覺風格**：採用 **中國傳統美學、傳統配色、優雅、溫潤，文人雅士風格** 結合暗色調設計。
*   **背景設計**：使用徑向漸層 (`radial-gradient`)，色彩範圍從中心深藍 (`hsl(210, 30%, 25%)` 等) 到邊緣純黑。
*   **容器比例**：所有遊戲或資料類型介面必須位於 `#stage` 內。
                確保在 `gameX.js` 中建立容器時，其類別包含 `gameX-overlay`，例如：
                div.className = 'gameX-overlay hidden';
                並掛載於 `document.getElementById('stage')` 之下。
*   **邊框設計**：使用微弱的白色半透明邊框 (`hsla(0, 0%, 100%, 0.20)`) 來定義區域。
*   **物件尺寸與移動速度**：所有物件尺寸與移動速度，都必須以邏輯像素 (`px`) 為單位。由於舞台容器整體被縮放，內部的物件只需維持固定的邏輯尺寸，預設 1u=16px。
請參考 `game6.js` 的 `setupCanvas` 函數範例：
        ```javascript
        setupCanvas: function () {
            const area = document.querySelector('.game6-area');
            // 邏輯畫布大小固定為容器偏移大小（在 500x850 舞台內）
            this.canvas.width = area.offsetWidth;
            this.canvas.height = area.offsetHeight;

            // 使用固定的 px 作為基本單位，配合 stage 的 transform: scale() 達成跨裝置適配
            this.u = 16; 
            // 初始化玩家尺寸
            this.player.width = this.ui.playerWidth * this.u;
            this.player.height = this.ui.playerHeight * this.u;
            this.player.x = this.canvas.width / 2;
        }
        ```
*   **字體尺寸**：一律使用 `px` 單位，避免受系統字體縮放影響。
*   **顏色**：使用 hsl 色彩模式，基準為 hsl(0, 60%, 24%)。
*   **透明度**：使用 hsla 色彩模式，基準為 hsla(0, 0%, 100%, 0.80)。

## 2. 欄位分配 (Layout)
一個標準的遊戲介面應包含以下垂直排列的區域：
1.  **頂部資訊列 (`gameX-header`)**：
    *   **左側**：分數看板 (`gameX-score-board`)。
    *   **右側**：控制按鈕組 (`gameX-controls`)，依序包含「難度標籤」按鈕 (`gameX-diff-tag`)、「重來」按鈕 (`gameX-retryGame-btn`)與「開新局」按鈕 (`gameX-newGame-btn`)。
    *   **備註**：控制按鈕組的位置與順序已統一，難度選擇器置於左側第一個。
2.  **生命值列 (`gameX-sub-header`)**：
    *   居中顯示紅心圖標 (`hearts`)，用於代表剩餘錯誤次數。
3.  **核心遊戲區 (`gameX-area`)**：
    *   **問題區域 (`gameX-question-area`)**：顯示詩詞題目、提示字或關鍵字選擇器。
    題目字數若超過7個字，需自動調整字體尺寸，避免超出界面寬度，請參考GAME1.js的 adjustFontSize 函數。
    *   **詩詞資訊 (`gameX-poem-info`)**：標註詩名(顯示長度限制15個中文字)、朝代、作者，具備連結樣式，點擊可開啟詩詞資料集。
4.  **答案區域 (`gameX-answer-area`)**：
    *   包含**計時方框/圓環 (`gameX-timer-ring`)**（使用 SVG 動態描繪）。
    *   **選項網格 (`gameX-answer-grid`)**：放置選擇按鈕。
5.  **遊戲結束訊息 (`gameX-message`)**：
    *   遊戲結束時顯示，包含標題、內容和綠色按鈕。

## 3. 命名規則
*   **ID 命名**：統一以 `gameX-` 為前綴（X 為遊戲編號），例如 `game1-container`, `game2-score`。
*   **CSS 類別**：遵循相同的 `gameX-` 前綴，通用組件如 `nav-btn`, `heart` 則維持跨遊戲統一。
*   **JS 物件**：將遊戲邏輯封裝在 `window.GameX` 物件中，並暴露 `show()`, `stopGame()` 等生命週期函數。
*   **檔案命名**：統一以 `gameX-` 為前綴（X 為遊戲編號），例如 `game1.css`, `game1.js`。
*   **各遊戲難度設定的變數名稱**：如果有使用到符合功能的變數請依下方統一命名
    *   `timeLimit`: 時間限制（秒）—— 已棄用於大多數遊戲，改用 `timeLimitRate`
    *   `timeLimitRate`: 每字時間倍率（秒/字），實際時限 = 執行期間實際題目字數 × timeLimitRate（⚠️ 必須在取詩/生題函式之後才能計算，不可用固定設定值如 `questionCount` 代替實際字數）
    *   `poemMinRating`: 最低詩詞評分
    *   `maxMistakeCount`: 最多錯誤次數
    *   `answerAtLine`: 答案出現在第幾行，0=第一行或第二行，1=第一行，2=第二行，3=第一行和第二行
    *   `minLines`: 最少詩句數
    *   `maxLines`: 最多詩句數
    *   `minChars`: 最少總字數
    *   `maxChars`: 最多總字數
    *   `minMaskCount`: 最少遮罩字數
    *   `maxMaskCount`: 最多遮罩字數
    *   `sentenceMinRating`: 最低詩句評分（game3 專用）
    *   `incrementSpeed`: 遊戲速度遞增量
    *   `maxSpeed`: 最高速度上限
    *   `timeMutiply`: 時間倍率（注意：此處為程式既有拼法，非 multiply）

### 3.1 ⚠️ CSS class 命名必須加模組前綴（強制要求）
**所有遊戲模組的專屬 CSS class，必須加上 `gameX-` 前綴。絕不使用通用名稱（如 `.poem-line`、`.title`、`.body`）作為某一遊戲的私有 class。**

例外允許跨遊戲共用的通用 class：`.hearts`、`.heart`、`.nav-btn`、`.hidden`、`.flying-star`（均由 `scoreManager.js` 或 `screen_adaptive.css` 定義）。

### 3.2 URL 參數嚴格比對機制
所有 `gameX.js` 結尾自動啟動的判斷，必須使用 `URLSearchParams` 精確比對，防止字串部分吻合（例如 `game=1` 與 `game=10`）造成的錯誤加載：
```javascript
if (new URLSearchParams(window.location.search).get('game') === 'X') {
    setTimeout(() => {
        if (window.GameX) window.GameX.show();
        window.history.replaceState({}, document.title, window.location.pathname);
    }, 50);
}
```

## 4. 動畫與交互設計
*   **計時器**：使用 SVG 矩形路徑 (`#gameX-timer-path`)，透過 `stroke-dashoffset` 實現邊框倒數動畫。
*   **錯誤反饋**：點選錯誤選項時，必須觸發 `shake` (抖動) 動畫。
*   **過關動畫**：獲勝時呼叫 `ScoreManager.playWinAnimation` 顯示得分飄浮與計分跳動效果。
*   **按鈕效果**：Hover 時應有背景色亮化或邊框變色。
    box-shadow: 0 0px 8px rgba(255, 255, 255, 0.9);
    filter: brightness(1.5);
*   **挑戰成功防呆機制**：一旦觸發通關 (Win)，必須**立刻**禁用 (`disabled = true`) 「重來」與「開新局」按鍵，防止玩家在動畫期間連點刷分。

## 5. 字體規劃與色彩習慣
*   **主字體**：`'Noto Serif TC', serif` (思源宋體)。
*   **動態縮放**：腳本中必須包含 `adjustFontSize` 邏輯，根據文字長度自動調整 `px` 大小。
*   **詩詞名稱長度限制**：詩詞名稱 poem-info只能顯示前12個字，若超過12個字，則以「...」替代。
        //詩詞名稱只能顯示前12個字
        let title = this.currentPoem.title;
        if (title.length > 12) {
            title = title.substring(0, 10) + "...";
        }
        const infoText = `${title} / ${this.currentPoem.dynasty} / ${this.currentPoem.author}`;
        const infoEl = document.getElementById('gameX-poem-info');
        infoEl.textContent = infoText;
        infoEl.dataset.poemId = this.currentPoem.id;
        //this.adjustFontSize(infoEl, infoText.length, 20, 1.0); //取消，若詩詞名稱過長，改以刪減詩詞名稱字數替代

*   **強調/關鍵字**：`hsl(36, 80%, 50%)` (金橙色)；
*   **紅心/損血**：`hsl(4, 90%, 60%)` (鮮紅色)；
*   **正確反饋**：`hsl(145, 68%, 25%)` (深翠綠色)；
*   **錯誤反饋**：`hsl(0, 70%, 25%)` (朱紅色)。

## 6. 共用題庫選取 (getSharedRandomPoem)

```javascript
// 完整簽名（在 script.js 中定義）
getSharedRandomPoem(
    minRating,        // 最低詩詞評分
    minLines,         // 最少行數（演算法強制從奇數句起始，保持兩句一聯完整性）
    maxLines,         // 最多行數
    minChars,         // 最少總字數
    maxChars,         // 最多總字數
    keyword = "",     // 篩選關鍵字（選填，空字串表示不限制）
    seed = null,      // 隨機種子/關卡序號（傳入則確定性選取，null 則純隨機）
    gameKey = ""      // 遊戲ID，融入種子確保不同遊戲同關卡產生不同題目
)
```

**回傳值：**
```javascript
{
    poem: 原始詩詞物件,           // 包含 id, title, dynasty, author, content 等欄位
    lines: ["乾淨句子1", ...],    // 已去除標點符號的字串陣列
    startIndex: 起始索引           // 於原始 poem.content 中的起始行號
}
// 無符合結果時回傳 null（內建降級保護，會自動放寬 minRating 重試）
```

**重要原則**：
- 全程禁止以 `rating` 排序選取，確保隨機性與題庫多樣性。
- 演算法限制必須從奇數句（index 0, 2, 4...）開始，保持「兩句一聯」語意完整。
- 有 `seed` 時為確定性選取，相同參數永遠回傳同一首詩（適合挑戰模式）。

---

## 7. 共通性元件規格與範例 (Common Component Specifications)

### 7.1 HTML 結構模板
```html
<div id="gameX-container" class="gameX-overlay hidden">
    <!-- 1. 資訊列 -->
    <div class="gameX-header">
        <div class="gameX-score-board">分數: <span id="gameX-score">0</span></div>
        <div class="gameX-controls">
            <!-- 難度標籤統一放在最左側 -->
            <button class="gameX-difficulty-tag" id="gameX-diff-tag">小學</button>
            <button id="gameX-retryGame-btn" class="nav-btn">重來</button>
            <button id="gameX-newGame-btn" class="nav-btn">開新局</button>
        </div>
    </div>
    
    <!-- 2. 生命值列 -->
    <div class="gameX-sub-header">
        <div id="gameX-hearts" class="hearts"></div>
    </div>

    <div id="gameX-area" class="gameX-area">
        <!-- 3. 問題區域 (自定義) -->
        ...
        
        <!-- 4. 答案與計時方框區域 -->
        <div class="gameX-answer-area">
            <div id="gameX-answer-grid-container" class="gameX-answer-grid-container">
                <!-- SVG 計時框 -->
                <svg id="gameX-timer-ring">
                    <rect id="gameX-timer-path" x="4" y="4"></rect>
                </svg>
                <!-- 答案網格 -->
                <div id="gameX-answer-grid" class="gameX-answer-grid">

                </div>
            </div>
        </div>
    </div>

    <!-- 5. 訊息覆蓋窗 -->
    <div id="gameX-message" class="gameX-message hidden">
        <h2 id="gameX-msg-title">時間到！</h2>
        <p id="gameX-msg-content"></p>
        <button id="gameX-msg-btn" class="nav-btn">再來一局</button>
    </div>
</div>
```

### 7.1.1 容器掛載與縮放監聽範例 (JS)
```javascript
createDOM: function () {
    const div = document.createElement('div');
    div.id = 'gameX-container';
    div.className = 'gameX-overlay hidden';
    div.innerHTML = `...`; // 放入上述 HTML 模板
    
    // 必須掛載於 stage 內，或透過 registerOverlayResize 同步 stage 位置
    document.getElementById('stage').appendChild(div);

    if (window.registerOverlayResize) {
        window.registerOverlayResize((r) => {
            // 同步舞台的置中位置與縮放比例
            div.style.left = r.left + 'px';
            div.style.top = r.top + 'px';
            div.style.width = '500px';
            div.style.height = '850px';
            div.style.transform = `scale(${r.scale})`;
            div.style.transformOrigin = 'top left';
        });
    }
}
```

### 7.2 CSS 樣式模板
```css
/* header 佈局 */
.gameX-header {
    padding: 2px 8px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(0, 0, 0, 0.4); /* 若為暗色系遊戲 */
    z-index: 10;
    width: 100%;
    height: 40px;
}
.gameX-score-board {
    color: hsl(50, 70%, 60%);
    font-size: 24px;
    font-weight: bold;
    padding-left: 40px;
}

/* 控制按鈕（重來／開新局） */
.gameX-controls .nav-btn {
    padding: 0px 10px;
    background: hsl(0, 60%, 25%);
    border: 0.6px solid hsla(40, 100%, 80%, 0.3);
    color: hsl(45, 80%, 70%);
    border-radius: 12px;
    font-size: 20px;
    cursor: pointer;
    transition: background 0.2s;
}

.gameX-controls .nav-btn:hover {
    filter: brightness(1.5);
}

.gameX-controls .nav-btn:disabled {
    background: hsl(0, 0%, 50%);
    color: hsl(45, 0%, 90%);
}

/* 難度標籤 */
.gameX-difficulty-tag {
    padding: 2px 6px;
    border-radius: 6px;
    font-size: 20px;
    color: hsl(45, 30%, 85%);
    border: 0.6px solid hsla(40, 100%, 80%, 0.1);
    z-index: 100;
    cursor: pointer;
    transition: all 0.2s;
}

.gameX-difficulty-tag[data-level="小學"] {
    background: linear-gradient(135deg, hsl(100, 40%, 40%) 0%, hsl(120, 40%, 30%) 100%);
}

.gameX-difficulty-tag[data-level="中學"] {
    background: linear-gradient(135deg, hsl(210, 40%, 50%) 0%, hsl(230, 40%, 40%) 100%);
}

.gameX-difficulty-tag[data-level="高中"] {
    background: linear-gradient(135deg, hsl(0, 50%, 50%) 0%, hsl(0, 50%, 35%) 100%);
}

.gameX-difficulty-tag[data-level="大學"] {
    background: linear-gradient(135deg, hsl(290, 50%, 50%) 0%, hsl(270, 50%, 35%) 100%);
}

.gameX-difficulty-tag[data-level="研究所"] {
    background: linear-gradient(135deg, hsl(50, 80%, 50%) 0%, hsl(40, 80%, 35%) 100%);
}

.gameX-difficulty-tag:hover {
    box-shadow: 0 0px 8px rgba(255, 255, 255, 0.9);
    filter: brightness(1.5);
}

/* sub-header (紅心區) */
.gameX-sub-header {
    display: flex;
    justify-content: center;
    align-items: center;
    background: rgba(0, 0, 0, 0.4);
    z-index: 9;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    width: 100%;
    height: 40px;
}
.hearts {
    display: flex;
    gap: 2px;
    justify-content: center;
    flex-wrap: wrap;
}

.gameX-hearts .heart {
    color: hsl(4, 90%, 60%);
    font-size: 32px;
}

/* 答案區域容器與 SVG 計時器 */
.gameX-answer-grid-container {
    position: relative;
    padding: 1px;
    border-radius: 20px; 
}
#gameX-timer-ring {
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 100%;
    pointer-events: none; /* 切勿阻擋按鈕點擊 */
}
#gameX-timer-path {
    fill: none;
    stroke: hsl(0, 80%, 36%);
    stroke-width: 10px;
    stroke-linecap: round;
    transition: stroke-dashoffset 0.1s linear;
}
/* 勝利失敗跳出訊息視窗 */
.gameX-message {
    position: absolute;
    top: 90%;
    left: 50%;
    height: 15%;
    width: 90%;
    transform: translate(-50%, -50%);
    background: rgba(255, 255, 255, 1);
    padding: 10px;
    padding-bottom: 60px;
    border-radius: 20px;
    text-align: center;
    color: hsl(0, 0%, 30%);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.8);
    z-index: 200;
}

.gameX-message h2 {
    font-size: 24px;
    font-weight: bold;
    color: hsl(145, 68%, 36%);
    display: inline-block;
    vertical-align: middle;
    margin-right: 10px;
    margin: 6px 0;
}

.gameX-message p {
    font-size: 24px;
    font-weight: bold;
    display: inline-block;
    vertical-align: middle;
    white-space: nowrap;
}

.gameX-message .nav-btn {
    position: absolute;
    bottom: 14px;
    left: 50%;
    transform: translateX(-50%);
    background: hsl(145, 68%, 36%);
    color: white;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.8);
    border: none;
    border-radius: 30px;
    padding: 4px 40px;
    font-size: 24px;
    cursor: pointer;
    transition: filter 0.5s;
}

.gameX-message .nav-btn:hover {
    filter: brightness(1.25);
}
```

### 7.3 JS 邏輯模板

**更新倒數方框 (Timer Ring Update)**
```javascript
updateTimerRing: function (ratio) {
    const rect = document.getElementById('gameX-timer-path');
    const container = document.getElementById('gameX-answer-grid-container');
    if (!rect || !container) return;

    const w = container.offsetWidth;
    const h = container.offsetHeight;
    
    const svg = document.getElementById('gameX-timer-ring');
    svg.setAttribute('width', w);
    svg.setAttribute('height', h);

    const rw = Math.max(0, w - 10);
    const rh = Math.max(0, h - 10);

    rect.setAttribute('width', rw);
    rect.setAttribute('height', rh);

    const perimeter = (rw + rh) * 2;
    rect.style.strokeDasharray = perimeter;
    rect.style.strokeDashoffset = perimeter * (1 - ratio); // ratio 從 1 至 0
}
```

**勝利或失敗判定**
取消 gamewin()與gamelose()，改用 `gameOver(win, reason)`

**防刷分與結算防呆**
```javascript
gameOver: function (win, reason) {
    this.isActive = false;
    this.isWin = win;
    
    if (win) {
        document.getElementById('gameX-retryGame-btn').disabled = true;
        document.getElementById('gameX-newGame-btn').disabled = true;
    } else {
        document.getElementById('gameX-retryGame-btn').disabled = false;
        document.getElementById('gameX-newGame-btn').disabled = false;
    }
    
    const msgDiv = document.getElementById('gameX-message');
    msgDiv.classList.remove('hidden');
}
```

---

## 8. 音樂音效共用模組（SoundManager）

所有音效皆由 `script.js` 的 `SoundManager` 模組提供，無需各遊戲自行建立 AudioContext。

```javascript
// --- 古箏與鋼琴單音 ---
soundManager.playGuzheng(index, gainMultiply = 1)     // 撥放古箏音階（index 為音階索引）
soundManager.playMelodyNote(noteNumber, gainMultiply = 1) // 撥放自然大調單音

// --- 基礎互動音效 ---
soundManager.playHit(index, timeLength)     // 撞擊聲
soundManager.playSuccess()                  // 正確回饋（A4 → A5，較長）
soundManager.playSuccessShort()             // 正確短音（適合連續點擊遊戲）
soundManager.playFailure()                  // 錯誤回饋（A3 → A2）
soundManager.playCountdown()               // 倒數321音效（G3 → A3 → A4）

// --- 物品操作音效 ---
soundManager.playOpenItem()                // 開啟項目（E4）
soundManager.playCloseItem()               // 關閉項目（C4）
soundManager.playConfirmItem()             // 確認項目（A4）

// --- 敵人/破壞音效 ---
soundManager.playBreakEnemy()             // 敵人被破壞（兩聲向上跳動）

// --- 情緒音效 ---
soundManager.playJoyfulTriple()           // 喜悅三連音（C5 E5 G5）
soundManager.playJoyfulTripleSlow()       // 喜悅三連音（慢速）
soundManager.playSadTriple()              // 悲傷三連音（A4 → E3 → C3）

// --- 樂曲播放系統 ---
melodyPlayer.setMelody(melodyName)        // 設定當前樂曲（不立即播放）
melodyPlayer.playNextNote()              // 播放樂曲的下一個音符（手動步進）
melodyPlayer.playFullMelody(melodyName)  // 連續自動播放整首樂曲
```

**內建樂曲庫**（傳入 `melodyName` 字串）：
`小星星`、`生日快樂歌`、`青青校樹`、`送別`、`茉莉花`、`踏雪尋梅`、`春神來了`、`望春風`

---

## 9. ScoreManager 分數與階級系統

**階級對照表**（由 `scoreManager.js` 定義）：

| 階級 | 中文名稱 | 最低分數 |
|------|---------|---------|
| 1 | 書僮 | 0 |
| 2 | 蒙童 | 10,000 |
| 3 | 塾生 | 20,000 |
| 4 | 童生 | 40,000 |
| 5 | 縣案首 | 80,000 |
| 6 | 府案首 | 160,000 |
| 7 | 文童 | 320,000 |
| 8 | 秀才 | 640,000 |
| 9 | 舉人 | 1,280,000 |
| 10 | 貢士 | 2,560,000 |
| 11 | 進士 | 5,120,000 |
| 12 | 探花 | 10,240,000 |
| 13 | 榜眼 | 20,480,000 |
| 14 | 狀元 | 40,960,000 |
| 15 | 大儒 | 81,920,000 |

**難度得分乘數**：小學×1、中學×2、高中×3、大學×4、研究所×5

**各遊戲分數設定**（`gameSettings` 物件）：
```javascript
// base: 基礎過關分、heart: 每條命價值、time: 每秒計時分、getPointA/B: 特定動作得分
'game1':  { base: 50,  heart: 10, time: 1 }
'game2':  { base: 100, heart: 10, time: 2, getPointA: 10 }
'game3':  { base: 100, heart: 10, time: 2, getPointA: 5 }
'game4':  { base: 100, heart: 10, time: 2, getPointA: 10 }
'game5':  { base: 100, heart: 10, time: 1, getPointA: 25 }
'game6':  { base: 100, heart: 10, time: 1, getPointA: 3 }
'game7':  { base: 100, heart: 10, time: 2, getPointA: 5 }
'game8':  { base: 100, heart: 10, time: 1, getPointA: 2 }
'game9':  { base: 100, heart: 10, time: 5, getPointA: 0.5 }
'game10': { base: 100, heart: 10, time: 0, getPointA: 1, getPointB: 20 }
'game11': { base: 100, heart: 10, time: 0, getPointA: 5, getPointB: 30 }
'game12': { base: 100, heart: 10, time: 2, getPointA: 20 }
'game13': { base: 100, heart: 10, time: 2, getPointA: 20 }
'game14': { base: 100, heart: 20, time: 5, getPointA: 10 }
```

---

> [!IMPORTANT]
> **開發提醒**：將這套標準範本應用於新遊戲之上，並確保位於 `#stage` 容器內，能大幅減少 CSS 定位錯誤以及縮放時的破圖問題，同時讓全系列遊戲享有統一的操作體驗與視覺習慣。新增 game15+ 時，同步更新：`menu.js` 捷徑列表、`scoreManager.js` 的 `gameSettings`、成就系統的遊戲解鎖驗證、`index.html` 的 `<script>` 與 `<link>` 引入。
